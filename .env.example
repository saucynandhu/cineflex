VITE_TMDB_API_KEY=your_tmdb_api_key_here

# Optional: authorized downloads API for media files your app is allowed to distribute.
# REST base format: https://example.com/api/downloads -> /movie/:tmdbId or /tv/:tmdbId/:season/:episode
# Template format is also supported, e.g. https://example.com/downloads/{type}/{tmdbId}?s={season}&e={episode}
VITE_DOWNLOADS_API_URL=
